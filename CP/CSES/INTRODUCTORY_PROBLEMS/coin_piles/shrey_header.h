#ifndef SHREY_HEADER      // include guard start
#define SHREY_HEADER

// Function declaration
void greet();
void fastIntInput(int* numPtr);
void fastIntOput(int n);
void fastStrOutput(const char *s);
void fastNewline();
void fastFlush();

// Macro definition
#define PI 3.14159

// Struct definition
struct Point {
    int x, y;
};

#endif
